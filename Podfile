# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'Tank' do
  pod 'Alamofire'
  pod 'SwiftSoup'
  
  target 'TankTests' do
      inherit! :search_paths
  end
  
  target 'TankUITests' do
       inherit! :search_paths
  end

end
